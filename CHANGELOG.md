# Changelog

## v1.0.3 (2026-07-20)

### 修复
- **服务启动页嵌入的启动脚本**：更新 service-not-found.html 里 base64 嵌入的 start_server.bat/.sh 为最新修复版（含 Step 4b 括号转义修复、pip/curl/镜像加固）。此前用户从插件下载按钮拿到的是旧版（含括号崩溃 bug），导致服务无法启动。

## v1.0.2 (2026-07-17)

### 修复
- **启动脚本**: 修正 Step 4b 括号未转义导致脚本静默崩溃; `pip` 改为 `python -m pip`; `curl` 加 `-f` 防止 HTTP 404 被当作成功; pip 安装依赖加镜像兜底与超时重试
- **iframe 文案**: 修复 DRC / Done / Total / Routing 等动态文案里 `${1}` 占位符不被替换的问题
- **多轮廓板布线**: 修复多轮廓板 + 板外焊盘时 `outline` 变量未绑定导致布线崩溃（本地补丁）
- **打包体积**: 修正 `.edaignore` 漏配，把 `.git/` 等开发文件排除，`.eext` 由 18MB 降到约 1MB

### 新增
- **DRC 默认值预填**: 打开布线对话框时按当前板子 DRC 规则，自动把线宽/间距/过孔/板边中违规的参数抬到最小合规值（已合规的不动）
- **任务中断**: 布线超时或取消时真正终止服务端任务（改用子进程模式 + 加长锁等待 + 客户端超时主动 cancel），避免僵尸任务占用 CPU、阻塞下一次布线

### 文档
- 重写 `CLAUDE.md`（修正端点、单位、路由模式、CI 发布流程等）

## v1.0.1 (2026-06-15)

多语言优化

## v1.0.0 (2026-05-11)

首个正式发布版本。

### 功能

- **单端布线** — 通过桥接服务器调用 KiCadRouting Tools A* 路由引擎
- **差分对布线** — 支持中心线路由、极性交换、GND 过孔放置
- **BGA/QFN 扇出** — 自动逃逸路径生成
- **电源平面** — 自动过孔连接 SMD 焊盘到内层铜皮
- **网络选择** — 支持按名称过滤、通配符匹配、排除模式
- **层配置** — 支持多层布线，每层可设置成本权重
- **参数配置 UI** — iframe 对话框，支持线宽、间距、过孔、网格步长等参数
- **异步布线** — 后台执行，支持进度轮询和取消
- **AI 分析接口** — 板卡概览、电源网络分析、差分对检测、总线组检测、网络统计
- **DRC 参数校验** — 提交前自动校验参数是否满足 EasyEDA DRC 规则
- **自动取消** — 提交新任务自动取消上一个正在运行的任务
- **大型元件分块传输** — 通过 /api/extra-components 分块发送，避免单次请求过大
